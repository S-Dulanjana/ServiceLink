import 'package:flutter/material.dart';

class PaintingDetails extends StatelessWidget {
  const PaintingDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("PaintingDetails"));
  }
}
