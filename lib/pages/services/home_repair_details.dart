import 'package:flutter/material.dart';

class HomeRepairDetails extends StatelessWidget {
  const HomeRepairDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("HomeRepairDetails"));
  }
}
