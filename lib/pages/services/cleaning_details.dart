import 'package:flutter/material.dart';

class CleaningDetails extends StatelessWidget {
  const CleaningDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("CleaningDetails"));
  }
}
