import 'package:flutter/material.dart';

class ElectricalDetails extends StatelessWidget {
  const ElectricalDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("ElectricalDetails"));
  }
}
